#!/usr/bin/env node
'use strict';

const { main } = require('../src/sandbox/microvmRunner');

main().then((code) => {
  process.exit(code);
}).catch((err) => {
  process.stderr.write(`${err.message}\n`);
  process.exit(1);
});
